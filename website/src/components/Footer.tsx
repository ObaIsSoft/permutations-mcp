import { Github } from 'lucide-react';

interface Genome {
    dnaHash: string;
    viabilityScore?: number;
    chromosomes: {
        ch1_structure: { topology: string };
        ch3_type_display: { family: string };
        ch5_color_primary: { hue: number };
        ch7_edge: { radius: number };
        ch8_motion: { physics: string };
        ch13_atmosphere: { fx: string };
        ch14_physics: { material: string };
        ch15_biomarker: { geometry: string };
    };
}

export function Footer({ genome }: { genome: Genome }) {
    const ch = genome.chromosomes;
    const hue = ch.ch5_color_primary.hue;

    const dnaFields = [
        { label: 'Topology', val: ch.ch1_structure.topology },
        { label: 'Motion', val: ch.ch8_motion.physics },
        { label: 'FX', val: ch.ch13_atmosphere.fx },
        { label: 'Material', val: ch.ch14_physics.material },
        { label: 'Biomarker', val: ch.ch15_biomarker.geometry },
        { label: 'Viability', val: `${Math.round((genome.viabilityScore ?? 1) * 100)}%` },
    ];

    return (
        <footer style={{ background: 'var(--color-surface)', borderTop: '1px solid rgba(255,255,255,0.06)' }}>
            <div className="max-w-7xl mx-auto px-6 md:px-12 py-16 md:py-20">

                {/* DNA attribution block */}
                <div className="mb-12 p-6 relative overflow-hidden"
                    style={{
                        border: '1px solid rgba(255,255,255,0.06)',
                        borderRadius: 'var(--edge-radius)',
                        background: `hsla(${hue}, 40%, 15%, 0.15)`,
                    }}>
                    <div className="absolute right-6 top-6 font-mono text-[80px] font-bold leading-none select-none pointer-events-none"
                        style={{ color: `hsla(${hue}, 60%, 55%, 0.06)` }}>Ψ</div>

                    <p className="dna-label mb-4" style={{ color: 'var(--color-primary)' }}>
                        ↳ This Website Was Generated By Permutations DNA
                    </p>

                    <p className="font-mono text-xs mb-6" style={{ color: 'rgba(255,255,255,0.3)', wordBreak: 'break-all' }}>
                        {genome.dnaHash}
                    </p>

                    <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-4">
                        {dnaFields.map(({ label, val }) => (
                            <div key={label}>
                                <div className="dna-label mb-1">{label}</div>
                                <div className="font-display font-bold uppercase text-white text-xs tracking-wider">{val}</div>
                            </div>
                        ))}
                    </div>
                </div>

                {/* Bottom row */}
                <div className="flex flex-col sm:flex-row items-center justify-between gap-6">
                    <div className="flex items-center gap-3">
                        <div className="w-6 h-6 flex items-center justify-center"
                            style={{ background: 'var(--color-primary)', borderRadius: 'var(--edge-radius)' }}>
                            <span className="font-display font-bold text-black text-[10px]">P</span>
                        </div>
                        <span className="font-display font-bold uppercase tracking-widest text-white text-sm">Permutations</span>
                        <span className="font-mono text-[10px]" style={{ color: 'rgba(255,255,255,0.2)' }}>v1.0.0</span>
                    </div>

                    <div className="flex items-center gap-6">
                        <a href="https://github.com/ObaIsSoft/Permutations" target="_blank" rel="noopener noreferrer"
                            className="flex items-center gap-2 font-mono text-xs transition-colors"
                            style={{ color: 'rgba(255,255,255,0.4)' }}
                            onMouseEnter={e => (e.currentTarget as HTMLElement).style.color = 'var(--color-primary)'}
                            onMouseLeave={e => (e.currentTarget as HTMLElement).style.color = 'rgba(255,255,255,0.4)'}>
                            <Github size={14} /> GitHub
                        </a>

                        <div className="flex items-center gap-2 font-mono text-[10px]" style={{ color: 'var(--color-primary)' }}>
                            <span className="relative flex h-1.5 w-1.5">
                                <span className="animate-ping absolute h-full w-full rounded-full bg-current opacity-75" />
                                <span className="relative h-1.5 w-1.5 rounded-full bg-current" />
                            </span>
                            System Online
                        </div>
                    </div>
                </div>

                <div className="mt-8 pt-6 text-center font-mono text-[10px] tracking-[0.3em] uppercase"
                    style={{ borderTop: '1px solid rgba(255,255,255,0.04)', color: 'rgba(255,255,255,0.2)' }}>
                    No Templates. No Slop. Only Math.
                </div>
            </div>
        </footer>
    );
}
